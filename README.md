# Dwarkesh Portfolio

## Setup

1. Install Node.js.
2. Run `npm install`.
3. Copy `.env.example` to `.env`.
4. Put a new Google App Password in `.env`.
5. Run `npm start`.
6. Open `http://localhost:3000`.

Do not upload `.env` to GitHub. The `.gitignore` file already excludes it.
